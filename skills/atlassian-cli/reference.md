<!-- GENERATED by atlassian-cli/refresh_skill_reference.py — do not edit.  Judgment and gotchas belong in SKILL.md; this file is pure API surface. -->

# Atlassian MCP tool reference

31 tools. `R` marks a required parameter. Regenerate with `atlassian-cli/refresh_skill_reference.py --update`.

## Contents

- addCommentToJiraIssue
- addWorklogToJiraIssue
- atlassianUserInfo
- createConfluenceFooterComment
- createConfluenceInlineComment
- createConfluencePage
- createIssueLink
- createJiraIssue
- editJiraIssue
- fetch
- getAccessibleAtlassianResources
- getConfluenceCommentChildren
- getConfluencePage
- getConfluencePageDescendants
- getConfluencePageFooterComments
- getConfluencePageInlineComments
- getConfluenceSpaces
- getIssueLinkTypes
- getJiraIssue
- getJiraIssueRemoteIssueLinks
- getJiraIssueTypeMetaWithFields
- getJiraProjectIssueTypesMetadata
- getPagesInConfluenceSpace
- getTransitionsForJiraIssue
- getVisibleJiraProjects
- lookupJiraAccountId
- search
- searchConfluenceUsingCql
- searchJiraIssuesUsingJql
- transitionJiraIssue
- updateConfluencePage

## addCommentToJiraIssue

Add or update a comment on a Jira issue

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `commentBody` | string | R | Comment body |
| `issueIdOrKey` | string | R | Issue ID or key (e.g., PROJ-123 or 10000) |
| `commentId` | string |  | ID of an existing comment to update |
| `commentVisibility` | object |  |  |
| `contentFormat` | `markdown` \| `adf` |  | Content format for body content |
| `responseContentFormat` | `markdown` \| `adf` |  | Content format for body content |

## addWorklogToJiraIssue

Add or update a worklog on a Jira issue. When worklogId is provided, updates that worklog;

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `issueIdOrKey` | string | R | Issue ID or key (e.g., PROJ-123 or 10000) |
| `timeSpent` | string | R | Time spent (e.g |
| `commentBody` | string |  | Comment body |
| `contentFormat` | `markdown` \| `adf` |  | Content format for body content |
| `started` | string |  | When the work was started (ISO 8601 date-time, e.g |
| `visibility` | object |  |  |
| `worklogId` | string |  | ID of an existing worklog to update |

## atlassianUserInfo

Get current user info

_No parameters._

## createConfluenceFooterComment

Create a footer comment on a page or blog post

| param | type | | notes |
|---|---|---|---|
| `body` | string | R | Page/comment body |
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `attachmentId` | string |  | Attachment ID to include |
| `contentFormat` | `html` \| `markdown` \| `adf` |  | Content format for page/comment body |
| `contentType` | `page` \| `blog` |  | Type of content: page or blog post |
| `customContentId` | string |  | Custom content ID to include |
| `pageId` | string |  | Page or blog post ID, or a Confluence tiny link ID (the encoded part from /wiki/x/ URLs, e.g., Fc1bBw) |
| `parentCommentId` | string |  | Parent comment ID for replies |

## createConfluenceInlineComment

Create an inline comment on specific text in a page or blog post. For top-level comments, provide pageId and inlineCommentProperties (text selection + match counts). For replies, provide parentCommentId only (no pageId or inlineCommentProperties). You must first fetch the page content (e.g. using getConfluencePage) to find the exact text and count how many times it appears. Incorrect or missing inlineCommentProperties can result in a 400 error from the Confluence API.

| param | type | | notes |
|---|---|---|---|
| `body` | string | R | Page/comment body |
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `contentFormat` | `html` \| `markdown` \| `adf` |  | Content format for page/comment body |
| `contentType` | `page` \| `blog` |  | Type of content: page or blog post |
| `inlineCommentProperties` | object |  | Required for top-level inline comments (not replies) |
| `pageId` | string |  | Page or blog post ID, or a Confluence tiny link ID (the encoded part from /wiki/x/ URLs, e.g., Fc1bBw) |
| `parentCommentId` | string |  | Parent comment ID for replies |

## createConfluencePage

Create a Confluence page or blog post

| param | type | | notes |
|---|---|---|---|
| `body` | string | R | Page/comment body |
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `spaceId` | string | R | Numeric space ID (a Long, e.g |
| `contentFormat` | `html` \| `markdown` \| `adf` |  | Content format for page/comment body |
| `contentType` | `page` \| `blog` |  | Type of content: page or blog post |
| `isPrivate` | boolean |  | Make private |
| `parentId` | string |  | ID of the parent to nest the new page under |
| `status` | `current` \| `draft` |  | Status (draft for unpublished) |
| `subtype` | `live` |  | Page subtype (pages only, ignored for blogs) |
| `title` | string |  | Page or blog post title |

## createIssueLink

Create a link between two Jira issues. For directional link types (e.g. Blocks): inwardIssue = issue that blocks, outwardIssue = issue that is blocked (e.g. "A is blocked by B" → inwardIssue: B, outwardIssue: A). Use getIssueLinkTypes if link type is unknown.

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `inwardIssue` | string | R | Inward issue key (e.g |
| `outwardIssue` | string | R | Outward issue key (e.g |
| `type` | string | R | Link type name (e.g |
| `comment` | string |  | Optional comment on the outward issue. |
| `contentFormat` | `markdown` \| `adf` |  | Content format for body content |

## createJiraIssue

Create a Jira issue. Use additional_fields to set any Jira field that does not have its own parameter (e.g. custom fields, priority, components).

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `issueTypeName` | string | R | Type (Task, Bug, Story) |
| `projectKey` | string | R | Project key |
| `summary` | string | R |  |
| `additional_fields` | object |  | REQUIRED for custom fields |
| `assignee_account_id` | string |  | Assignee ID |
| `contentFormat` | `markdown` \| `adf` |  | Content format for body content |
| `description` | string/object |  | Issue description |
| `parent` | string |  | Parent for subtasks |
| `responseContentFormat` | `markdown` \| `adf` |  | Content format for body content |
| `transition` | object |  | Optional workflow transition to apply during creation |

## editJiraIssue

Update issue. After updating, returns the issue using the same default read fields as getJiraIssue (summary, description, status, issuetype, priority, labels, components, assignee, reporter, created, updated, resolution, project). Use getJiraIssue with fields ["*all"] or a custom list if you need more.

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `fields` | object | R | Fields to set, keyed by field name or customfield_* ID |
| `issueIdOrKey` | string | R | Issue ID or key (e.g., PROJ-123 or 10000) |
| `contentFormat` | `markdown` \| `adf` |  | Content format for body content |
| `responseContentFormat` | `markdown` \| `adf` |  | Content format for body content |

## fetch

Get details of a Jira issue or Confluence page by ARI (Atlassian Resource Identifier), if the id is not an ARI, then use a different tool to fetch the content

| param | type | | notes |
|---|---|---|---|
| `id` | string | R | Atlassian Resource Identifier (ARI) from search results, e.g., "ari:cloud:jira:cloudId:issue/10107" or "ari:cloud:confluence:cloudId:page/123456789" |
| `cloudId` | string |  | Not needed for this tool — cloudId is extracted from the ARI automatically |

## getAccessibleAtlassianResources

Get cloudId to make tool calls. When a link is provided (e.g. https://site.atlassian.net/*), try passing the site hostname (e.g. site.atlassian.net) as cloudId to other tools first; if that fails, use this tool to list accessible resources.

_No parameters._

## getConfluenceCommentChildren

Get reply(child) comments for a comment

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `commentId` | string | R | Parent comment ID |
| `commentType` | `footer` \| `inline` | R | Comment type |
| `contentFormat` | `markdown` \| `adf` |  | Content format for body content |
| `cursor` | string |  | Pagination cursor |
| `limit` | number |  | Max replies |
| `sort` | `id` \| `-id` \| `created-date` \| `-created-date` |  | Sort order |

## getConfluencePage

Get a Confluence page or blog post by ID, including body content.

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `pageId` | string | R | Page or blog post ID, or a Confluence tiny link ID (the encoded part from /wiki/x/ URLs, e.g., Fc1bBw) |
| `contentFormat` | `html` \| `markdown` \| `adf` |  | Content format for page/comment body |
| `contentType` | `page` \| `blog` |  | Type of content: page or blog post |

## getConfluencePageDescendants

Get child pages of specified page

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `pageId` | string | R | Page or blog post ID, or a Confluence tiny link ID (the encoded part from /wiki/x/ URLs, e.g., Fc1bBw) |
| `cursor` | string |  | Pagination cursor |
| `depth` | number |  | Max depth |
| `limit` | number |  | Max descendants |

## getConfluencePageFooterComments

Get footer comments for a page or blog post

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `pageId` | string | R | Page or blog post ID, or a Confluence tiny link ID (the encoded part from /wiki/x/ URLs, e.g., Fc1bBw) |
| `contentFormat` | `markdown` \| `adf` |  | Content format for body content |
| `contentType` | `page` \| `blog` |  | Type of content: page or blog post |
| `cursor` | string |  | Pagination cursor |
| `includeReplies` | boolean |  | When true, also return reply comments |
| `limit` | number |  | Max comments |
| `repliesPerComment` | integer |  |  |
| `sort` | `id` \| `-id` \| `created-date` \| `-created-date` |  | Sort order |
| `status` | `current` \| `archived` \| `trashed` \| `deleted` \| `historical` \| `draft` |  | Comment status |

## getConfluencePageInlineComments

Get inline comments for a page or blog post

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `pageId` | string | R | Page or blog post ID, or a Confluence tiny link ID (the encoded part from /wiki/x/ URLs, e.g., Fc1bBw) |
| `contentFormat` | `markdown` \| `adf` |  | Content format for body content |
| `contentType` | `page` \| `blog` |  | Type of content: page or blog post |
| `cursor` | string |  | Pagination cursor |
| `includeReplies` | boolean |  | When true, also return reply comments |
| `limit` | number |  | Max comments |
| `repliesPerComment` | integer |  |  |
| `resolutionStatus` | `resolved` \| `open` \| `dangling` \| `reopened` |  | Resolution status |
| `sort` | `id` \| `-id` \| `created-date` \| `-created-date` |  | Sort order |
| `status` | `current` \| `archived` \| `trashed` \| `deleted` \| `historical` \| `draft` |  | Comment status |

## getConfluenceSpaces

Get spaces

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `expand` | string/array |  | Properties to expand |
| `favoritedBy` | string |  | User favorites |
| `favourite` | boolean |  | Favorite spaces only |
| `ids` | string/array |  | Space IDs |
| `keys` | string/array |  | Space keys |
| `labels` | string/array |  | Space labels |
| `limit` | number |  | Max results (default: 25, max: 250) |
| `start` | number |  | Start index |
| `status` | `current` \| `archived` |  | Space status |
| `type` | `global` \| `personal` |  | Space type |

## getIssueLinkTypes

Get available Jira issue link types (e.g. Blocks, Duplicate, Clones, Relates). For createIssueLink: inwardIssue = blocker, outwardIssue = blocked (e.g. "A is blocked by B" → inwardIssue: B, outwardIssue: A).

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |

## getJiraIssue

Get issue details

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `issueIdOrKey` | string | R | Issue ID or key (e.g., PROJ-123 or 10000) |
| `expand` | string |  | Additional issue details to expand in the response (for example, renderedFields or names). |
| `failFast` | boolean |  | Whether to fail quickly when some fields cannot be loaded instead of partially returning data. |
| `fields` | array |  | Issue fields to return |
| `fieldsByKeys` | boolean |  | Whether fields in the request are referenced by field keys instead of IDs. |
| `properties` | array |  | Issue property keys to include in the response. |
| `responseContentFormat` | `markdown` \| `adf` |  | Content format for body content |
| `updateHistory` | boolean |  | Whether to update the user's recently viewed projects/history for this issue request. |

## getJiraIssueRemoteIssueLinks

Get remote links

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `issueIdOrKey` | string | R | Issue ID or key (e.g., PROJ-123 or 10000) |
| `globalId` | string |  |  |

## getJiraIssueTypeMetaWithFields

Get field metadata

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `issueTypeId` | string | R |  |
| `projectIdOrKey` | string | R |  |
| `maxResults` | number |  |  |
| `requiredFieldsOnly` | boolean |  | When true (default), returns only the fields that are required to create an issue of this type, reducing response size |
| `startAt` | number |  |  |

## getJiraProjectIssueTypesMetadata

Get issue types

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `projectIdOrKey` | string | R |  |
| `maxResults` | number |  |  |
| `startAt` | number |  |  |

## getPagesInConfluenceSpace

Get pages or blog posts in a space

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `spaceId` | string | R | Space ID |
| `contentFormat` | `markdown` \| `adf` |  | Content format for body content |
| `contentType` | `page` \| `blog` |  | Type of content: page or blog post |
| `cursor` | string |  | Pagination cursor |
| `limit` | integer |  | Max results (default: 25, max: 250) |
| `sort` | `id` \| `-id` \| `created-date` \| `-created-date` \| `modified-date` \| `-modified-date` \| `title` \| `-title` |  | Sort order |
| `status` | `current` \| `archived` \| `deleted` \| `trashed` |  | Page status |
| `title` | string |  | Title filter |

## getTransitionsForJiraIssue

Get transitions

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `issueIdOrKey` | string | R | Issue ID or key (e.g., PROJ-123 or 10000) |
| `expand` | string |  |  |
| `includeUnavailableTransitions` | boolean |  |  |
| `skipRemoteOnlyCondition` | boolean |  |  |
| `sortByOpsBarAndStatus` | boolean |  |  |
| `transitionId` | string |  |  |

## getVisibleJiraProjects

Get projects

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `action` | `view` \| `browse` \| `edit` \| `create` |  |  |
| `expandIssueTypes` | boolean |  |  |
| `maxResults` | number |  |  |
| `searchString` | string |  |  |
| `startAt` | number |  |  |

## lookupJiraAccountId

Lookup user IDs

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `searchString` | string | R |  |

## search

Search Jira and Confluence using Rovo Search, ALWAYS use this tool to search for Jira and Confluence content unless the word CQL or JQL is used in the context

| param | type | | notes |
|---|---|---|---|
| `query` | string | R | The search query to use for Rovo Search |
| `cloudId` | string |  | Not needed for this tool — cloudId is derived from your access token automatically |

## searchConfluenceUsingCql

Search Confluence content (pages, blog posts, comments, attachments) using CQL (Confluence Query Language). CQL is specific to Confluence and is not interchangeable with JQL.

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `cql` | string | R | CQL (Confluence Query Language) query string |
| `cqlcontext` | string |  | CQL context for narrowing search scope (e.g., spaceKey) |
| `cursor` | string |  | Pagination cursor |
| `expand` | string |  | Properties to expand |
| `limit` | number |  | Max results (default: 25, max: 250) |
| `next` | boolean |  | Include next page link |
| `prev` | boolean |  | Include previous page link |

## searchJiraIssuesUsingJql

Search issues with JQL, total counts only when explicitly requested.

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `jql` | string | R | JQL query |
| `fields` | array |  | Issue fields to return |
| `maxResults` | number |  | Max (50-100) |
| `nextPageToken` | string |  | Page token |
| `responseContentFormat` | `markdown` \| `adf` |  | Content format for body content |
| `searchResultMode` | `issues` \| `count` \| `all` |  | Default "issues" |

## transitionJiraIssue

Transition issue status

| param | type | | notes |
|---|---|---|---|
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `issueIdOrKey` | string | R | Issue ID or key (e.g., PROJ-123 or 10000) |
| `transition` | object | R |  |
| `fields` | object |  |  |
| `historyMetadata` | object |  |  |
| `update` | object |  |  |

## updateConfluencePage

Update a Confluence page or blog post

| param | type | | notes |
|---|---|---|---|
| `body` | string | R | Page/comment body |
| `cloudId` | string | R | Cloud ID (UUID or site URL) |
| `pageId` | string | R | Page or blog post ID, or a Confluence tiny link ID (the encoded part from /wiki/x/ URLs, e.g., Fc1bBw) |
| `contentFormat` | `html` \| `markdown` \| `adf` |  | Content format for page/comment body |
| `contentType` | `page` \| `blog` |  | Type of content: page or blog post |
| `includeBody` | boolean |  | If true, include the page body in the response; if false, omit it to reduce payload size. |
| `parentId` | string |  | ID of the parent to move the page under |
| `spaceId` | string |  | Space ID |
| `status` | `current` \| `draft` |  | Page status |
| `title` | string |  | New title |
| `versionMessage` | string |  | Version message |

